# server port
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vengatesanns/online-restaurant-system.git
          searchPaths: '{application}'