pipeline {
    agent any
    stages {
        stage('Build') {
             steps {
                    echo ">>>>> Build Service Registry..."
                    sh 'pwd'
                    sh 'rm -r ./*'
                    sh 'cd ./restaurant-system/microservices/service-registry/'
                    sh 'mvn clean install'
             }
        }
    }
}