pipeline {
    agent any
    stages {
        stage("Build") {
             steps {
                script {
                    echo ">>>>> Build Service Registry..."
                    mvn clean install
                }
             }
        }
    }
}