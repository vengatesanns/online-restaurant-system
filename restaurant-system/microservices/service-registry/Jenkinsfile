pipeline {
    agent any
    stages {
        stage('Build') {
             steps {
                    echo ">>>>> Build Service Registry..."
                    sh 'mvn clean install'
             }
        }
    }
}